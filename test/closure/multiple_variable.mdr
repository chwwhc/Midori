var makeAdder = closure(var a, var b) 
{
  return closure(var x) { return a + b + x; };
};

var adder = makeAdder(3, 4);

fixed main = closure()
{
	PrintLine(adder(5));  // Should print 12

	return 0;
};