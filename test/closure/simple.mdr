fixed main := closure() : Integer
{
	var x := closure(var y : Integer) : () -> Integer 
	{
		return closure() : Integer { return y; };
	};
	var f := x(5);
	PrintLine(f()); // Should print 5

	return 0;
};