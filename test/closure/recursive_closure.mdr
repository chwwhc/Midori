var makeRecursive = closure(n) {
  if (n == 0) return closure(x) { return 1; };
  return closure(x) { return x * makeRecursive(n - 1)(x - 1); };
};
var factorial = makeRecursive(5);
PrintLine(factorial(5));  // Should print 120
