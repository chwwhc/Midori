var makeAdder = closure(var a) 
{
  return closure(var b) 
  {
    return closure(var c) 
    {
        return a + b + c;
    };
  };
};

var adder = makeAdder(3)(4);

fixed main = closure()
{
    PrintLine(adder(5));  // Should print 12

    return 0;
};
