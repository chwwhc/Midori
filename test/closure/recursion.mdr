var factorial = closure(var n : Integer) : Integer
{
  if (n <= 1) return 1;
  return n * factorial(n-1);
};

fixed main = closure() : Integer
{
	PrintLine(factorial(5));  // Should print 120
	return 0;
};
