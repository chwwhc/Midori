var x = closure(var y) 
{
  return closure(var z) { y = y - 1; return y + z; };
};

var f = x(5);

fixed main = closure()
{
	PrintLine(f(2)); // Should print 6
	PrintLine(f(2)); // Should print 5

	return 0;
};
