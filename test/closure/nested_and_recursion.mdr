var createCounter = closure(initial) {
  var counter = closure(value) {
    initial = initial + value;
    return closure() { return initial; };
  };
  return counter;
};
var counter = createCounter(5);
var next = counter(3);
PrintLine(next());  // Should print 8
