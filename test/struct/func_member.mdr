struct Fib
{
	N : Integer;
	fn : (Integer) -> Integer;
}

fixed main := closure() : Integer
{

	fixed fib := new Fib(35, closure(fixed n : Integer) : Integer {
		return n < 2 ? n : fib.fn(n - 1) + fib.fn(n - 2);
	});

	PrintLine(fib.fn(fib.N));

    return 0;
};