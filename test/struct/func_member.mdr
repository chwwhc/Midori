struct Fib
{
	N : Int;
	fn : (Int) -> Int;
}

fixed main = fn() : Int
{

	fixed fib = new Fib(35, fn(fixed n : Int) : Int {
		return n < 2 ? n : fib.fn(n - 1) + fib.fn(n - 2);
	});

	PrintLine(fib.fn(fib.N));

    return 0;
};