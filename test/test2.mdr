#include "E:\Projects\Midori\MidoriUtil\MidoriUtil.mdr"

fixed TestFib = closure() : Unit
{

	fixed fib = closure(fixed n : Integer) : Integer
	{
		if (n <= 1) return n;
		return fib(n - 1) + fib(n - 2);
	};

	fixed start = GetTime();

	fixed fib_35 = fib(35);

	fixed end = GetTime();

	PrintLine("Benchmark took " ++ ((end - start) as Text) ++ " milliseconds");

	return ();
};