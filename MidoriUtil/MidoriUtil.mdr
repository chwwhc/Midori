namespace IO
{
	foreign "Print" Print : (Text) -> Unit;

	fixed PrintLine = fn(fixed text : Text) : Unit
	{
		Print(text);
		Print("\n");
		return ();
	};
}

namespace DateTime
{
	foreign "GetTime" GetTime : () -> Frac;
}